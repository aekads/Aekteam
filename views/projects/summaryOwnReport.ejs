<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Work Summary</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <!-- Chart.js CDN -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <!-- jQuery CDN -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        /* Custom font for better aesthetics */
        body {
            font-family: 'Inter', sans-serif;
        }
        /* Scrollbar customization for a cleaner look */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }
        ::-webkit-scrollbar-thumb {
            background: #888;
            border-radius: 10px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #555;
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800 min-h-screen flex flex-col">

   

    <!-- Main Content Area -->
    <div id="main-content" class="flex-1 md:ml-64 p-5 transition-all duration-300 ease-in-out">
        <!-- Hamburger menu for mobile -->
        <button id="openSidebar" class="md:hidden text-3xl cursor-pointer p-3 absolute left-5 top-4 bg-blue-600 text-white rounded-lg hover:bg-blue-700 shadow-md focus:outline-none">
            &#9776;
        </button>

        <div class="container max-w-full mx-auto p-6 bg-white rounded-xl shadow-lg text-center mt-16 md:mt-0">
            <h1 class="text-3xl font-semibold text-gray-800 mb-5 mt-0 flex items-center justify-center">
                <i class="fas fa-chart-pie mr-3 text-blue-600"></i> My Work Hours Summary
            </h1>

            <!-- Project Filter Dropdown -->
            <div class="mb-6">
                <label for="project" class="sr-only">Select Project</label>
                <select id="project" class="w-full p-3 border-2 border-blue-500 rounded-lg text-base mb-5 cursor-pointer bg-blue-50 hover:border-blue-700 transition-colors duration-300 focus:ring-2 focus:ring-blue-400 focus:border-transparent shadow-sm">
                    <option value="">All Projects</option>
                  <% if (projects && projects.length > 0) { %>
                        <% projects.forEach(proj => { %>
                            <option value="<%= proj.project_id %>"><%= proj.project_name %></option>
                        <% }); %>
                    <% } else { %>
                        <option value="" disabled>No projects available</option>
                    <% } %> 
                </select>
            </div>

            <!-- Work Summary Chart -->
            <div class="bg-white rounded-xl shadow-md p-4 mb-8">
                <canvas id="workSummaryChart" class="w-full h-auto"></canvas>
            </div>

            <h2 class="text-2xl font-semibold text-gray-800 mt-10 mb-4 flex items-center justify-center">
                <i class="fas fa-calendar-alt mr-3 text-blue-600"></i> Date-wise Work Summary
            </h2>

            <!-- Date-wise Work Summary Table -->
            <div class="overflow-x-auto">
                <table class="w-full border-collapse bg-white rounded-xl shadow-lg">
                    <thead>
                        <tr>
                            <th class="py-4 px-6 bg-blue-600 text-white text-left text-lg rounded-tl-xl">Date</th>
                            <th class="py-4 px-6 bg-blue-600 text-white text-left text-lg">Project</th>
                            <th class="py-4 px-6 bg-blue-600 text-white text-left text-lg">Work Description</th>
                            <th class="py-4 px-6 bg-blue-600 text-white text-left text-lg rounded-tr-xl">Work Hours</th>
                        </tr>
                    </thead>
                    <tbody id="reportTableBody">
                        <!-- Data will be loaded here by JavaScript -->
                        <tr><td colspan="4" class="py-4 px-6 text-center text-gray-600">Loading data...</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <script>
        $(document).ready(function () {
            const projSelect = $("#project");
            let workChart = null; // Variable to hold the Chart.js instance

            // Sidebar toggle logic
            const sidebar = $('#sidebar');
            const mainContent = $('#main-content');
            const openSidebarBtn = $('#openSidebar');
            const closeSidebarBtn = $('#closeSidebar');

            openSidebarBtn.on('click', function() {
                sidebar.removeClass('-translate-x-full');
                // No need to adjust mainContent margin for mobile, as it's full width
            });

            closeSidebarBtn.on('click', function() {
                sidebar.addClass('-translate-x-full');
            });

            // Adjust main content margin on window resize for desktop view
            $(window).on('resize', function() {
                if ($(window).width() >= 768) { // md breakpoint in Tailwind is 768px
                    mainContent.addClass('md:ml-64');
                    sidebar.removeClass('-translate-x-full'); // Ensure sidebar is visible on desktop
                } else {
                    mainContent.removeClass('md:ml-64');
                    sidebar.addClass('-translate-x-full'); // Ensure sidebar is hidden on mobile
                }
            }).trigger('resize'); // Trigger on load to set initial state

            /**
             * Calculates the number of days in the current month.
             * @returns {string[]} An array of day numbers (e.g., ["1", "2", ..., "31"]).
             */
            function getFullMonthDays() {
                let today = new Date();
                // Get the last day of the current month (day 0 of next month)
                let daysInMonth = new Date(today.getFullYear(), today.getMonth() + 1, 0).getDate();
                // Create an array of strings for each day of the month
                return Array.from({ length: daysInMonth }, (_, i) => (i + 1).toString());
            }

            /**
             * Fetches work summary data from the backend and updates the chart and table.
             */
            function fetchSummary() {
                const projectId = projSelect.val(); // Get the selected project ID

                // AJAX call to fetch data
                $.ajax({
                    url: "/dashboard/employee/projects/get-own-summary", // Backend API endpoint
                    method: "GET",
                    data: { project_id: projectId }, // Pass selected project ID as a query parameter
                    success: function (data) {
                        console.log("Raw Data from API:", data);

                        const daysInMonth = getFullMonthDays();
                        let hoursByDay = {};
                        // Initialize all days of the current month with 0 hours
                        daysInMonth.forEach(day => (hoursByDay[day] = 0));

                        let groupedData = {}; // To group data by date for the table

                        // Process fetched data
                        data.forEach(entry => {
                            // Extract day from the date (e.g., "2024-07-17" -> 17)
                            let day = new Date(entry.date).getDate().toString();
                            // Accumulate hours for each day
                            hoursByDay[day] += parseFloat(entry.hours) || 0;

                            // Group entries by date for the detailed table
                            if (!groupedData[entry.date]) {
                                groupedData[entry.date] = [];
                            }
                            groupedData[entry.date].push(entry);
                        });

                        // Convert the hoursByDay object into an array ordered by day
                        let hoursArray = daysInMonth.map(day => hoursByDay[day]);

                        // Render the chart and table with the processed data
                        renderChart(daysInMonth, hoursArray);
                        renderTable(groupedData);
                    },
                    error: function (xhr, status, error) {
                        console.error("Error fetching data:", status, error);
                        // Display an error message in the table
                        $("#reportTableBody").html(`<tr><td colspan="4" class="py-4 px-6 text-center text-red-500">Error loading data. Please try again.</td></tr>`);
                        // Destroy existing chart if any, to clear previous data
                        if (workChart) {
                            workChart.destroy();
                            workChart = null;
                        }
                    }
                });
            }

            /**
             * Renders or updates the bar chart with work hours data.
             * @param {string[]} dates - Labels for the x-axis (days of the month).
             * @param {number[]} hours - Data for the y-axis (total hours for each day).
             */
            function renderChart(dates, hours) {
                const ctx = document.getElementById("workSummaryChart").getContext("2d");
                // Destroy any existing chart instance before creating a new one
                if (workChart) {
                    workChart.destroy();
                }

                workChart = new Chart(ctx, {
                    type: "bar", // Bar chart type
                    data: {
                        labels: dates, // Days of the month as labels
                        datasets: [{
                            label: "Total Work Hours",
                            data: hours, // Work hours data
                            backgroundColor: "rgba(54, 162, 235, 0.7)", // Blue color with transparency
                            borderColor: "rgba(54, 162, 235, 1)", // Solid blue border
                            borderWidth: 1,
                            borderRadius: 6 // Rounded bars
                        }]
                    },
                    options: {
                        responsive: true, // Chart resizes with the container
                        maintainAspectRatio: false, // Allows flexible height
                        plugins: {
                            legend: {
                                display: true,
                                position: 'top',
                                labels: {
                                    font: {
                                        size: 14
                                    }
                                }
                            },
                            tooltip: {
                                callbacks: {
                                    label: function(context) {
                                        let label = context.dataset.label || '';
                                        if (label) {
                                            label += ': ';
                                        }
                                        if (context.parsed.y !== null) {
                                            label += context.parsed.y.toFixed(2) + ' hrs';
                                        }
                                        return label;
                                    }
                                }
                            }
                        },
                        scales: {
                            x: {
                                title: {
                                    display: true,
                                    text: "Day of the Month",
                                    font: { size: 16, weight: 'bold' },
                                    color: '#555'
                                },
                                grid: {
                                    display: false // Hide x-axis grid lines
                                },
                                ticks: {
                                    font: { size: 12 }
                                }
                            },
                            y: {
                                beginAtZero: true, // Start y-axis from zero
                                title: {
                                    display: true,
                                    text: "Total Hours Worked",
                                    font: { size: 16, weight: 'bold' },
                                    color: '#555'
                                },
                                ticks: {
                                    font: { size: 12 }
                                },
                                grid: {
                                    color: 'rgba(200, 200, 200, 0.2)' // Light grid lines
                                }
                            }
                        }
                    }
                });
            }

            /**
             * Renders the detailed work summary table.
             * @param {Object} groupedData - Data grouped by date, containing an array of entries for each date.
             */
            function renderTable(groupedData) {
                let html = "";

                if (Object.keys(groupedData).length === 0) {
                    // Display message if no records are found
                    html = `<tr><td colspan="4" class="py-4 px-6 text-center text-gray-600">No records found for the selected project.</td></tr>`;
                } else {
                    // Iterate through each date and its associated entries
                    Object.keys(groupedData).sort().forEach(date => { // Sort by date for chronological order
                        groupedData[date].forEach(entry => {
                            // Format the date for display
                            let formattedDate = new Date(entry.date).toLocaleDateString("en-GB", {
                                year: "numeric",
                                month: "short",
                                day: "2-digit"
                            });

                            // Append a new table row for each entry
                            html += `<tr class="hover:bg-gray-100 transition-colors duration-150">
                                <td>${formattedDate}</td>
                                <td>${entry.project_name || "N/A"}</td> <!-- Use project_name from backend -->
                                <td>${entry.work_description || "No description"}</td>
                                <td class="font-semibold text-blue-700">${parseFloat(entry.hours).toFixed(2)} hrs</td>
                            </tr>`;
                        });
                    });
                }

                // Update the table body with the generated HTML
                $("#reportTableBody").html(html);
            }

            // Event listener for project selection change
            projSelect.change(fetchSummary);

            // Initial data fetch when the page loads
            fetchSummary();

            // Mock data for demonstration if the backend is not available
            // In a real application, remove this mock data and ensure your backend endpoint works.
            // This ensures the frontend displays something even without a live backend.
            // To use mock data, uncomment the following block and comment out the $.ajax call in fetchSummary().
            /*
            const mockProjects = [
                { project_id: "project1", project_name: "Project Alpha" },
                { project_id: "project2", project_name: "Project Beta" },
                { project_id: "project3", project_name: "Project Gamma" },
                { project_id: "project4", project_name: "Project Delta" }
            ];

            // Populate project dropdown with mock data
            mockProjects.forEach(proj => {
                $("#project").append(`<option value="${proj.project_id}">${proj.project_name}</option>`);
            });

            function fetchSummaryMock() {
                const projectId = projSelect.val();
                let mockData = [
                    { date: "2025-07-01", project_name: "Project Alpha", work_description: "Initial setup", hours: "4.5" },
                    { date: "2025-07-01", project_name: "Project Beta", work_description: "Meeting with client", hours: "2.0" },
                    { date: "2025-07-02", project_name: "Project Alpha", work_description: "Backend development", hours: "6.0" },
                    { date: "2025-07-03", project_name: "Project Gamma", work_description: "UI/UX design", hours: "5.5" },
                    { date: "2025-07-04", project_name: "Project Alpha", work_description: "Bug fixing", hours: "3.0" },
                    { date: "2025-07-05", project_name: "Project Beta", work_description: "Documentation", hours: "4.0" },
                    { date: "2025-07-08", project_name: "Project Delta", work_description: "Testing phase 1", hours: "7.0" },
                    { date: "2025-07-09", project_name: "Project Alpha", work_description: "Feature implementation", hours: "8.0" },
                    { date: "2025-07-10", project_name: "Project Gamma", work_description: "Design review", hours: "2.5" },
                    { date: "2025-07-11", project_name: "Project Beta", work_description: "Deployment prep", hours: "6.5" },
                    { date: "2025-07-12", project_name: "Project Delta", work_description: "Client presentation", hours: "3.0" },
                    { date: "2025-07-15", project_name: "Project Alpha", work_description: "Performance optimization", hours: "7.5" },
                    { date: "2025-07-16", project_name: "Project Gamma", work_description: "User feedback integration", hours: "4.0" },
                    { date: "2025-07-17", project_name: "Project Beta", work_description: "Final checks", hours: "5.0" }
                ];

                // Filter mock data based on selected project
                const filteredData = projectId ? mockData.filter(entry => {
                    const project = mockProjects.find(p => p.project_id === projectId);
                    return project && entry.project_name === project.project_name;
                }) : mockData;

                const daysInMonth = getFullMonthDays();
                let hoursByDay = {};
                daysInMonth.forEach(day => (hoursByDay[day] = 0));

                let groupedData = {};

                filteredData.forEach(entry => {
                    let day = new Date(entry.date).getDate().toString();
                    hoursByDay[day] += parseFloat(entry.hours) || 0;

                    if (!groupedData[entry.date]) {
                        groupedData[entry.date] = [];
                    }
                    groupedData[entry.date].push(entry);
                });

                let hoursArray = daysInMonth.map(day => hoursByDay[day]);

                renderChart(daysInMonth, hoursArray);
                renderTable(groupedData);
            }

            // Uncomment the line below and comment out projSelect.change(fetchSummary); and fetchSummary(); to use mock data
            // projSelect.change(fetchSummaryMock);
            // fetchSummaryMock();
            */
        });
    </script>

</body>
</html>
